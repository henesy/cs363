MATCH (enrollment)<-[:Covers]-(course)-[:Taking]-(offering_student)-[r]-(faculty)-[:F2P]-(person) 
RETURN person.Name, collect(offering_student.Classification), collect(offering_student.GPA), collect(enrollment)

